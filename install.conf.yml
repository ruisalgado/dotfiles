- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.vim: vim/
    ~/.vimrc: vimrc
    ~/.oh-my-zsh: oh-my-zsh
    ~/.zsh: zsh/
    ~/.zshrc:
        force: true
        path: zshrc
    ~/.gitconfig: gitconfig

- shell:
    - git submodule update --init --recursive
